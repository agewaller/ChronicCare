[project]
name = "mibyou-diary"
version = "0.1.0"
description = "未病ダイアリー - AI-powered preventive health diary with prompt/program separation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "pydantic>=2.10.0",
    "anthropic>=0.42.0",
    "openai>=1.50.0",
    "google-genai>=0.3.0",
    "pyyaml>=6.0",
    "jinja2>=3.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.25.0",
    "httpx>=0.28.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
